<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent" android:layout_height="fill_parent"
	android:id="@id/empty">

	<ProgressBar android:id="@id/empty_progress"
		android:layout_centerInParent="true" android:layout_width="20dp" android:layout_margin="10dp"
		android:layout_height="20dp" android:layout_toLeftOf="@id/list_header_text" />

	<TextView android:id="@id/empty_text" android:gravity="center"
		android:layout_below="@id/empty_progress" android:layout_width="fill_parent"
		android:layout_height="wrap_content" android:text="载入中..."
		android:textSize="16sp" android:paddingLeft="5dp"/>

</RelativeLayout>
